# LMS PWA Implementation Plan (fb_dd0741bd4fb5 + fb_d72b457c3b94)

> **For agentic workers:** REQUIRED SUB-SKILL: superpowers:executing-plans. Steps use checkbox (`- [ ]`).

**Goal:** Сделать LMS installable PWA (manifest + icons + SW) + ненавязчивую install-подсказку (pill на десктопе, iOS-инструкция).

**Architecture:** `app/manifest.ts` (static export) + PNG-иконки из `icon.svg` (sharp) + минимальный `public/sw.js` + client-компоненты `PwaRegister`/`InstallPrompt` в layout + чистый `lib/pwa.ts`.

**Tech Stack:** Next 16 static export, sharp (icon gen), vitest env=node, dictionaries i18n.

**Spec:** `docs/superpowers/specs/2026-06-17-lms-pwa-design.md`

---

### Task 1: `lib/pwa.ts` хелперы (pure, TDD)

**Files:** Create `LMS/tochka-sborki/web/lib/pwa.ts` + `lib/pwa.test.ts`

- [ ] Failing tests: `isIos()` true для iPhone UA, false для desktop; SSR-гард (no window)→false; `isInStandaloneMode()` true когда `matchMedia('(display-mode: standalone)').matches`.
- [ ] `vitest run lib/pwa.test.ts` → FAIL.
- [ ] Implement `isIos`, `isInStandaloneMode` (SSR-safe гарды).
- [ ] `vitest run` → PASS.

### Task 2: `app/manifest.ts` (TDD)

**Files:** Create `app/manifest.ts` + `app/manifest.test.ts`

- [ ] Failing test: `manifest()` → name 'Точка Сборки', display 'standalone', start_url '/', icons include 192 & 512 sizes, one maskable.
- [ ] `vitest run app/manifest.test.ts` → FAIL.
- [ ] Implement `manifest(): MetadataRoute.Manifest` per spec.
- [ ] `vitest run` → PASS.

### Task 3: иконки (sharp gen script)

**Files:** Create `scripts/gen-pwa-icons.mjs`; output `public/icon-192.png`, `public/icon-512.png`, `public/icon-maskable-512.png`

- [ ] Script: render `app/icon.svg` → 192/512 `any`; maskable = svg scaled ~80% centered on `#0a0a0f` 512 canvas.
- [ ] Run `node scripts/gen-pwa-icons.mjs`; verify 3 PNGs exist (non-zero size).

### Task 4: service worker + register

**Files:** Create `public/sw.js`, `components/pwa/pwa-register.tsx`

- [ ] `public/sw.js`: versioned CACHE; install precaches shell; activate prunes old; fetch = network-first, fallback cache then offline; skipWaiting+clients.claim.
- [ ] `pwa-register.tsx`: 'use client', register `/sw.js` on load behind `'serviceWorker' in navigator`; returns null.

### Task 5: install prompt + i18n

**Files:** Create `components/pwa/install-prompt.tsx`; Modify `lib/dictionaries.ts`

- [ ] `dictionaries.ts`: add `pwa: { install, installing, iosHint, dismiss }` to interface + ru + en.
- [ ] `install-prompt.tsx`: beforeinstallprompt capture → pill → prompt(); iOS hint; hide if standalone/dismissed (localStorage); RU+EN.

### Task 6: layout wiring + gates

**Files:** Modify `app/layout.tsx`

- [ ] metadata `manifest` + `appleWebApp`; `viewport` export with `themeColor`; mount `<PwaRegister/>` + `<InstallPrompt/>` in body.
- [ ] Gates: `vitest run` green; web `tsc --noEmit`; workers `tsc --noEmit`; `wrangler deploy --dry-run`; `next build` → assert `out/manifest.webmanifest`, `out/icon-512.png`, `out/sw.js` exist.
- [ ] Commit (branch→ff-merge→push); `fb.mjs status` both tickets done + build; memory write-back.

## Self-review
- Spec coverage: manifest ✓(T2), icons ✓(T3), SW ✓(T4), register ✓(T4), install-prompt+iOS ✓(T5), layout ✓(T6), tests ✓(T1-2).
- Type consistency: `MetadataRoute.Manifest` from 'next'; dictionaries `pwa` shape matches both locales.
- Gotcha 2: new files NOT consumed by workers (app/lib/components only) — confirm via workers tsc in T6.
- YAGNI: no push, no full offline, no per-skin icons.
