name = "tochka-sborki-api"
main = "src/index.ts"
compatibility_date = "2025-01-01"

[vars]
OWNER_EMAIL = "mamaev.sasha@gmail.com"

# Логи воркера (Resend message-id/статус) — чтобы отлаживать доставку magic-link, а не вслепую
[observability]
enabled = true

[[d1_databases]]
binding = "DB"
database_name = "tochka-sborki-db"
database_id = "c904db4d-a900-4ff1-80ae-6056c150ca53"

[[routes]]
pattern = "ai.mamaev.coach/api/*"
zone_name = "mamaev.coach"

[triggers]
crons = ["0 16 * * *"]
