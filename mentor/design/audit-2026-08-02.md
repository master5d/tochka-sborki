# design-audit: mentor (mentor.mamaev.coach) — 2026-08-02

Повод: сплошной проход по поверхностям после аудита hub.

## Инструментальные проверки

| Шаг | Результат |
|---|---|
| Rule Zero | не требуется: чужих брендов/спеков в задаче нет |
| Token lint (semgrep) | **0 findings** ✅ — но см. Finding 6: правила прошли мимо реального нарушения |
| Identity contract | **absent** — `design/identity.json` нет |
| Diagram lint | `design/diagrams`, `design/dataviz` пусты |
| Живой рендер | Playwright, 1440px, светлая тема (системная) |

## Findings

| # | Severity | Где | Что | Фикс |
|---|---|---|---|---|
| 1 | **HIGH** | `components/visuals.tsx:11-13` | Компонент написан **только под тёмную тему**: цвета зашиты константами — `text = 'rgba(255,255,255,0.4)'`, `accent = '#00d1ff'`, `background: 'rgba(0,0,0,0.2)'`. На светлой теме белый текст по светлому фону. **Поправка к первому замеру:** цифры 1.0–1.15 были артефактом — измеритель брал полупрозрачную плашку как фон, не смешивая её с родительским. После композитинга слоёв худшее значение — **2.84**, всего **22 узла ниже порога AA**. Текст не «невидим», но читается плохо | перевести на токены `--text-secondary` / `--text-accent` / `--border-color` / `--bg-surface` |
| 2 | **HIGH** | `components/visuals.tsx:16` + hero-раскладка | Та же панель **переполняет вьюпорт на +139px** → у страницы появляется горизонтальный скролл (`scrollWidth > innerWidth`) | ограничить панель шириной колонки; SVG растягивать по контейнеру, не по фиксированному viewBox |
| 3 | **MEDIUM** | `components/home-page.tsx` (7 мест), `visuals.tsx` | **22 разных кегля**, из них **26 текстовых узлов мельче 11px** (есть 8px и 8.8px). Это ниже читаемого минимума, а не «микро-лейбл» | ввести шкалу-токены (как в hub) и поднять пол до 12px |
| 4 | **MEDIUM** | глобально | **6 радиусов**: 2px, 3px, 4px, 20px, 50%, 100px при токене `--radius: 4px` | свести к `var(--radius)` + pill там, где нужен круг |
| 5 | **LOW** | `mentor/design/` | `identity.json` отсутствует → контракт не проверяется | снять контракт с фактической ДНК |
| 6 | **LOW** (advisory) | правила DesOps | Semgrep дал **0 findings**, хотя в `visuals.tsx` цвета зашиты хардкодом. Правила ловят литералы в разметке, но не **константы в JS** (`const accent = '#00d1ff'`) — слепая зона линта | расширить правило на объявления переменных со цветовыми литералами |

## 5-angle self-audit

- **Governance** — ⚠️ Findings 1, 4: `visuals.tsx` живёт мимо токен-слоя целиком; радиусы разъехались.
  Остальная разметка на CSS-переменных, как предписывает DESIGN.md.
- **Contrast** — ❌ **22 узла ниже AA**, худшее значение 2.84 (после исправления методики —
  см. поправки в конце файла). Панель в hero на светлой теме читается плохо, но не пуста.
- **Hierarchy** — ⚠️ Finding 3: разрыв шкалы. Заголовки 144/128/86px против тела 8–16px;
  между ними пусто. Вложенных карточек нет ✅.
- **Density** — ⚠️ Панель обсервабилити декоративна: рисует «мониторинг» без реальных данных,
  при этом занимает место и ломает раскладку. Tufte: data-ink без data.
- **Alignment** — ❌ Finding 2: горизонтальное переполнение страницы, чего нет ни на одной
  другой поверхности проекта.

## Вердикт

**FAIL** — 6 findings (2 high, 2 medium, 2 low).

Ключевое: на **светлой теме** hero-панель нечитаема и ломает раскладку. Поскольку тема по
умолчанию системная, значительная часть посетителей видит именно этот вариант.


---

## Polish-проход (тот же день)

| # | Было | Стало | Проверено |
|---|---|---|---|
| 1 | `visuals.tsx` мимо токенов: `rgba(255,255,255,0.4)`, `#00d1ff`, `rgba(0,0,0,0.2)` | все цвета из токен-слоя, SVG получил `role="img"` + `aria-label` | контраст обеих тем |
| 2 | панель переполняла вьюпорт на +139px | `max-width:100%`, `box-sizing`, SVG по ширине контейнера, трекинг ужат | `scrollWidth > innerWidth` = **false** |
| 3 | 22 кегля, 26 узлов мельче 11px (8px и 8.8px) | шкала-токены `--text-xs/sm/base/lg/xl`, 34 замены | ничего мельче 12px |
| 4 | 6 радиусов (2/3/4/20/50%/100px) | 2px и 3px → `var(--radius)`; остались 4px + намеренные pill/круг | живой DOM |
| 5 | `identity.json` отсутствовал | контракт снят с ДНК (density 6 — B2B плотнее лендинга) | `test-identity.ps1` = **ok** |
| 6 | акцент светлой темы `#0098bc` → 2.84; на плашке `rgba(accent,0.05)` даже `#0070c0` давал 4.33 | `#0063ab` (+ синхронизирован `--text-accent-rgb`, он отставал и красил плашки старым оттенком) | **0 нарушений AA** в light и dark |

### Две поправки к собственной методике

1. **Композитинг фона.** Первый замер считал полупрозрачную плашку непрозрачным фоном
   и выдавал контраст 1.0 там, где реально 2.84. Исправлено: слои смешиваются до
   непрозрачного родителя, альфа текста тоже учитывается.
2. **Ожидание кадра.** Замер сразу после программной смены `data-theme` возвращал старые
   значения (ложные 2.92 у футер-ссылок). Исправлено: два `requestAnimationFrame` перед сканом.

Гейты после правок: token lint **0 findings**, `tsc` чисто, build зелёный,
контраст **0/0** (light/dark), горизонтального переполнения нет.

**Вердикт после polish: PASS.**
