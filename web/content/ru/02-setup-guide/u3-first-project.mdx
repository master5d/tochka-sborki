---
title: "Первый проект"
unit: 3
module: 2
duration: "12 мин"
---

<Phase type="activation">

Есть ли у тебя папка, где ты хранишь рабочие файлы?

Если да — где она? Есть ли там какой-нибудь проект, пусть даже маленький?

Если нет — какой проект ты хотел бы начать?

</Phase>

<Phase type="reflection">

Claude Code работает в контексте папки. Это важно:

Чем организованнее структура твоих файлов — тем точнее агент понимает что происходит и что от него хотят.

CLAUDE.md — это инструкция для агента, которая лежит в корне проекта. Без неё агент каждый раз начинает «с нуля».

</Phase>

<Phase type="concept">

## CLAUDE.md — память агента для проекта

CLAUDE.md — это Markdown-файл, который Claude Code читает автоматически при каждом запуске в папке.

### Минимальный шаблон

```markdown
# Project Context

## Моя роль
[Кто я, что я знаю, мой опыт]

## Проект
[Что это за проект, зачем нужен]

## Стек
- Node.js 18
- [другие технологии]

## Структура папок
- `/src` — основной код
- `/my-experiments` — эксперименты

## Как запустить
[Команды для старта]

## Что делегировать Claude
- Написание кода по ТЗ
- Тесты
- Документирование

## Что решаю сам
- Архитектурные решения
- Финальная проверка качества
```

### Создание проекта

```bash
mkdir -p ~/my-project
cd ~/my-project
git init
touch CLAUDE.md
mkdir -p src my-experiments
```

### Советы по CLAUDE.md

- Держи до **200 строк** — длиннее снижает качество следования инструкциям
- Будь конкретным: `«Используй 2-пробельный отступ»` лучше чем `«форматируй правильно»`
- Обновляй когда меняется архитектура или стек

</Phase>

<Phase type="practice">

Создай свой первый проект:

```bash
mkdir -p ~/my-ai-project
cd ~/my-ai-project
git init
touch CLAUDE.md
mkdir -p my-experiments
```

Заполни CLAUDE.md по шаблону — напиши про реальный проект или задачу, над которой хочешь работать.

Затем запусти Claude Code в этой папке:

```bash
claude
```

Задай агенту: `«Прочитай CLAUDE.md и скажи, что ты теперь знаешь о проекте»`

Сохрани результат в `my-experiments/u3-claude-reads-context.md`

</Phase>
