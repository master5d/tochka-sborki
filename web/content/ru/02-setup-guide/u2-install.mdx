---
title: "Установка инструментов"
unit: 2
module: 2
duration: "15 мин"
---

<Phase type="activation">

Ты уже проверил окружение. Теперь главный вопрос:

Что ты ожидаешь от Claude Code? Что он должен делать для тебя?

Напиши одно-два предложения — не то, что правильно, а то, что ты реально хочешь получить.

</Phase>

<Phase type="reflection">

Большинство людей устанавливают инструменты без понимания — зачем.

Потом они не используют половину возможностей, потому что не знали, что они есть.

Твой ответ из Активации — это контекст. Держи его в голове пока настраиваешь.

</Phase>

<Phase type="concept">

<OsToggle />

## Установка Claude Code

Claude Code — это CLI (command-line interface), агент который работает прямо в терминале.

### Установка

```bash
npm install -g @anthropic-ai/claude-code
```

> 💡 `npm install -g` одинаково работает в bash и в PowerShell.

### Проверка

```bash
claude --version
claude --help
```

### Первый запуск

<OsBlock os="mac">

```bash
cd ~/Documents
claude
```

</OsBlock>

<OsBlock os="windows">

```powershell
cd $HOME\Documents
claude
```

</OsBlock>

Claude Code откроет интерактивную сессию. Первый раз попросит войти через браузер.

### Git: базовая конфигурация

Команды одинаковы на всех системах:

```bash
git config --global user.name "Твоё имя"
git config --global user.email "твоя@почта.com"

# Проверь:
git config --global user.name
```

### Полезные настройки терминала

<OsBlock os="mac">

```bash
# Добавь в ~/.bashrc или ~/.zshrc — полезные алиасы
alias ll='ls -lah'
alias gst='git status'
alias gp='git push'
```

> 💡 После добавления алиасов: `source ~/.bashrc` (или перезапусти терминал).

</OsBlock>

<OsBlock os="windows">

PowerShell использует профиль вместо `.bashrc`. Открой файл профиля:

```powershell
# Создать, если ещё нет
if (!(Test-Path $PROFILE)) { ni $PROFILE -ItemType File -Force }
# Открыть в редакторе
notepad $PROFILE
```

Добавь функции (PowerShell не любит алиасы с параметрами, поэтому используем функции):

```powershell
function gst { git status }
function gp  { git push }
function ll  { Get-ChildItem -Force }
```

> 💡 После сохранения: `. $PROFILE` (точка-пробел) или перезапусти PowerShell.

</OsBlock>

</Phase>

<Phase type="practice">

Выполни по порядку:

1. Установи Claude Code: `npm install -g @anthropic-ai/claude-code`
2. Проверь: `claude --version`
3. Настрой Git: `git config --global user.name "Твоё имя"`
4. Запусти Claude Code в любой папке:

<OsBlock os="mac">

```bash
cd ~/Documents && claude
```

</OsBlock>

<OsBlock os="windows">

```powershell
cd $HOME\Documents; claude
```

</OsBlock>

Когда Claude Code откроется — задай ему вопрос: `«Что ты умеешь делать?»`

Сохрани скриншот ответа в `my-experiments/u2-claude-first-run.png`

</Phase>
