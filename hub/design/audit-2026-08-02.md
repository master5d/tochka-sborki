# design-audit: hub (mamaev.coach) — 2026-08-02

Повод: владелец прислал скриншот нечитаемого лендинга (чёрное по чёрному) и попросил
причесать. Баг темы починен отдельно (`7110f29b`); этот аудит — состояние ПОСЛЕ фикса.

## Инструментальные проверки

| Шаг | Результат |
|---|---|
| Rule Zero (веб-верификация брендов) | не требуется: чужих брендов/спеков в задаче нет |
| Token lint (semgrep, 5 правил, 40 файлов) | **0 findings** ✅ |
| Identity contract (`test-identity.ps1`) | **absent** — `design/identity.json` отсутствует |
| Diagram lint (`design/diagrams`, `design/dataviz`) | пусто, проверять нечего |
| Живой рендер | Playwright: 1440 / 900 / 390 px, обе темы |

## Findings

| # | Severity | Где | Что | Фикс |
|---|---|---|---|---|
| 1 | **HIGH** | `components/home-page.tsx:52` | H1 задан inline `clamp(3rem, 11vw, 9rem)` в обход токена `--display-size: clamp(2.8rem, 7vw, 6.5rem)`. На 1440px даёт 144px, и строка «АЛЕКСАНДР» **не помещается**: `scrollWidth 1141 > clientWidth 1036` — визуально буква срезана. На 900px и 390px совпадает | вернуть на токен `--display-size` (или понизить верх clamp до ~7rem и дать `overflow-wrap`) |
| 2 | **MEDIUM** | глобально | Типографская лестница расползлась: в живом DOM **10+ размеров**, из них дробные `11.2 / 11.52 / 12.8 / 13.6 / 14.4 / 15.2 / 17.6px`. Стандарт: 12/14/16/20/24/32 | свести к шкале; дробные приходят от `rem`-множителей в inline-стилях |
| 3 | **MEDIUM** | `components/site-header.tsx` | Радиусы: `4px` (8 узлов) + `3px` (4 узла) + pill. Токен `--radius: 4px`, значение `3px` — дрейф | заменить `3px` → `var(--radius)` |
| 4 | **LOW** | `hub/design/` | `identity.json` отсутствует → контракт идентичности не проверяется автоматикой | сгенерировать контракт (dials/палитра) при следующем design-проходе |
| 5 | **LOW** | `components/lang-suggest-banner.tsx` | Языковой баннер занимает первый экран НАД sticky-шапкой: на 900px это ~2 полосы до контента | сделать баннер компактнее либо не sticky-перекрывающим hero |

## 5-angle self-audit

- **Governance** — ⚠️ Finding 1: hero обходит собственный токен размера. Остальное соответствует
  DESIGN.md (палитра, Geist, `--radius 4px`, inline-стили на переменных — как задокументировано).
- **Contrast** — ✅ чисто. Автоматический обход всех текстовых узлов в тёмной теме: **0 нарушений**
  WCAG AA (порог 4.5 для обычного текста, 3.0 для крупного). Светлая тема — токены те же,
  что покрыты новым гвардом `lib/a11y/contrast.test.ts` (4 состояния).
- **Hierarchy** — ⚠️ Finding 2: вложенных карточек нет ✅, но шкала кеглей размыта дробными
  значениями — лестница читается как «много близких размеров», а не как ступени.
- **Density** — ✅ чисто. Лендинг воздушный (`--section-gap 5rem`), data-ink разумный,
  клаттера нет; карточки проектов не вложены.
- **Alignment** — ⚠️ Finding 3: радиусы 4/3/pill вместо одного `--radius`. Сетка 4px в целом
  соблюдается; горизонтального переполнения страницы нет ни на одной из трёх ширин
  (`scrollWidth == innerWidth`), кроме внутреннего клипа H1 (Finding 1).

## Вердикт

**FAIL** — 5 findings (1 high, 2 medium, 2 low).

Единственная поломка, видимая пользователю, — Finding 1 (обрезанное имя в шапке страницы).
Остальное — гигиена системы, не аварии.

Передать в `/design-polish`: findings 1-3. Findings 4-5 — owner-решение.


---

## Polish-проход (тот же день)

| # | Было | Стало | Проверено |
|---|---|---|---|
| 1 | H1 inline `clamp(3rem,11vw,9rem)` → 144px, клип 105px | `var(--display-size)` → 100.8px, letter-spacing −0.04em, `overflow-wrap` | `scrollWidth <= clientWidth` = true на 1440/900/390 |
| 2 | 10+ кеглей, дробные 11.2/11.52/12.8/13.6/14.4/15.2/17.6 | шкала-токены `--text-xs/sm/base/lg/xl` (12/14/16/20/24); 16 замен + `--section-label-size` на токен | живой DOM: 12/14/16/20 + заголовочные 30.4/40/48/100.8 |
| 3 | радиусы 4px + 3px + pill | 3px → `var(--radius)` (4 узла) | живой DOM: только 4px и pill |
| 4 | `identity.json` отсутствовал | контракт снят с фактической ДНК (dials 4/2/3, обе палитры) | `test-identity.ps1` = **ok** |
| 5 | языковой баннер ~2 полосы до контента | padding/gap/кегль ужаты | высота баннера 50px |
| 6 (найдено при полировке) | hero-glow: центр `18%` уходил за левый край, `overflow:hidden` резал его в прямоугольник; на светлой теме читался как серое пятно | центр `32%`, радиус 60/50%, альфа на светлой 0.08 → 0.045 | глазами: кромки нет, свечение почти незаметно |

Гейты после правок: token lint **0 findings**, hub-тесты **24 passed**, `tsc` чисто,
контраст в тёмной теме — 0 нарушений AA.

**Вердикт после polish: PASS.**
