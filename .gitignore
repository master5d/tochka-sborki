# Scratch / temp
.tmp/
.superpowers/

# Diagnostic artifacts (Lighthouse runs, audit screenshots) — regenerated on demand
docs/**/report.html
docs/**/report.json
docs/**/*.png

# Архив исходников/référence-материалов курса (docx-опросники и т.п.) — не для VCS
LMS/tochka-sborki/_archive/

# Stray tool caches at repo root
node_modules/

# Личные заметки владельца под академию-манифест (репо PUBLIC — заметки не для VCS)
academy/_notes/*
!academy/_notes/README.md
# агентские droppings + локальные логи desops (аудит 2026-08-13)
.serena/
logs/

# --- секреты: канон лаборатории (аудит 2026-08-21) ---
# Перечисление (.env / .env.local / .env.*.local) пропускает .env.bak-<stamp>,
# .env.production и .dev.vars — в estate-hub так висел бэкап с живым ключом
# гейтвея. Ловим семейства целиком, примеры возвращаем негацией.
.env*
!.env.example
.dev.vars*
!.dev.vars.example
